from item import Item

init_items = {
    'outside':  [
        Item("torch", "a sturdy torch that will last as long as needed", True),
        Item("small stone", "a small stone")
    ],

    'foyer':    [],

    'overlook': [
        Item("large stone", "not quite a boulder")
    ],

    'narrow':   [],

    'treasure': [
        Item("shiny stone", "very, very shiny", False, True)
    ],
}